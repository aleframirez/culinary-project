const {
  validRole,
  validEmail,
  existUserId,
  existPostById,
  existCommentId,
  existCategorieId,
  allowedCollections,
} = require("./db-validators");

module.exports = {
  validRole,
  validEmail,
  existUserId,
  existPostById,
  existCommentId,
  existCategorieId,
  allowedCollections,
};
